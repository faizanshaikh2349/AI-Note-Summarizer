<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_variant"
    tools:context=".HomeActivity">

    <!-- Fragment host — leaves room for the floating nav -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="96dp" />

    <!--
        Floating Material 3 Bottom Navigation
        ─────────────────────────────────────
        • Side + bottom margins create the "pill floating above content" look
        • All-corner radius on bg_bottom_nav gives a true floating card shape
        • elevation="20dp"  → deep shadow behind the nav bar
        • translationZ keeps it above any card shadows
    -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="14dp"
        android:background="@drawable/bg_bottom_nav"
        android:elevation="20dp"
        android:translationZ="20dp"
        style="@style/ModernBottomNav"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        app:itemIconSize="24dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>